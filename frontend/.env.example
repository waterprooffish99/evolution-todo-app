# Frontend Environment Configuration
# Copy this file to .env.local and fill in your actual values

# ============================================
# Backend API URL
# ============================================
# URL where FastAPI backend is hosted
NEXT_PUBLIC_API_URL=http://localhost:8000

# Production example:
# NEXT_PUBLIC_API_URL=https://api.todo.yourapp.com

# ============================================
# Better Auth Configuration
# ============================================
# CRITICAL: Must match backend BETTER_AUTH_SECRET exactly
# This secret is used to SIGN JWT tokens (frontend)
# and VERIFY JWT tokens (backend)
BETTER_AUTH_SECRET=your-super-secret-key-min-32-characters-long-change-in-production

# Production example (generate secure secret):
# BETTER_AUTH_SECRET=openssl rand -base64 32

# URL where this Next.js app is hosted
# Better Auth uses this for token URLs and callbacks
BETTER_AUTH_URL=http://localhost:3000

# Production example:
# BETTER_AUTH_URL=https://todo.yourapp.com

# ============================================
# Database Configuration
# ============================================
# CRITICAL: Must be SAME database as backend
# Better Auth creates/manages the 'users' table
# Backend owns the 'tasks' table
# Both tables share user_id foreign key
DATABASE_URL=postgresql://neondb_owner:password@ep-xyz.region.aws.neon.tech/neondb?sslmode=require

# Get your Neon database URL:
# 1. Go to https://console.neon.tech
# 2. Create project/database
# 3. Copy Connection String
# 4. Paste here (replace placeholder above)

# ============================================
# Notes
# ============================================
# - NEVER commit .env.local to git (add to .gitignore)
# - Use separate DATABASE_URL for dev/staging/production branches
# - Rotate BETTER_AUTH_SECRET in production regularly
# - Better Auth will auto-create 'users' table on first run
# - Build errors about database adapter are EXPECTED (see BETTER_AUTH_SETUP.md)
